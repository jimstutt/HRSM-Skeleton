{-# LANGUAGE OverloadedStrings #-}

module Main where

import Reflex.Dom

main :: IO ()
main = mainWidget $ do
  el "h1" $ text "NGOLogisticsCG — WASM Reactor Online"
  el "p" $ text "This WASM build uses Reflex-DOM and GHC wasm32-wasi."

